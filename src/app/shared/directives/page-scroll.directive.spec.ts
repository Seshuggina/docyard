import { PageScrollDirective } from './page-scroll.directive';

describe('PageScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PageScrollDirective();
    expect(directive).toBeTruthy();
  });
});
